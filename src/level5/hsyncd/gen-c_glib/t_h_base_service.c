/**
 * Autogenerated by Thrift Compiler (0.12.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
#include <string.h>
#include <thrift/c_glib/thrift.h>
#include <thrift/c_glib/thrift_application_exception.h>
#include "t_h_base_service.h"

gboolean
t_h_base_service_if_exists (THBaseServiceIf *iface, gboolean* _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->exists (iface, _return, table, tget, io, error);
}

gboolean
t_h_base_service_if_exists_all (THBaseServiceIf *iface, GArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->exists_all (iface, _return, table, tgets, io, error);
}

gboolean
t_h_base_service_if_get (THBaseServiceIf *iface, TResult ** _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get (iface, _return, table, tget, io, error);
}

gboolean
t_h_base_service_if_get_multiple (THBaseServiceIf *iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get_multiple (iface, _return, table, tgets, io, error);
}

gboolean
t_h_base_service_if_put (THBaseServiceIf *iface, const GByteArray * table, const TPut * tput, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->put (iface, table, tput, io, error);
}

gboolean
t_h_base_service_if_check_and_put (THBaseServiceIf *iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TPut * tput, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->check_and_put (iface, _return, table, row, family, qualifier, value, tput, io, error);
}

gboolean
t_h_base_service_if_put_multiple (THBaseServiceIf *iface, const GByteArray * table, const GPtrArray * tputs, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->put_multiple (iface, table, tputs, io, error);
}

gboolean
t_h_base_service_if_delete_single (THBaseServiceIf *iface, const GByteArray * table, const TDelete * tdelete, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->delete_single (iface, table, tdelete, io, error);
}

gboolean
t_h_base_service_if_delete_multiple (THBaseServiceIf *iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tdeletes, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->delete_multiple (iface, _return, table, tdeletes, io, error);
}

gboolean
t_h_base_service_if_check_and_delete (THBaseServiceIf *iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TDelete * tdelete, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->check_and_delete (iface, _return, table, row, family, qualifier, value, tdelete, io, error);
}

gboolean
t_h_base_service_if_increment (THBaseServiceIf *iface, TResult ** _return, const GByteArray * table, const TIncrement * tincrement, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->increment (iface, _return, table, tincrement, io, error);
}

gboolean
t_h_base_service_if_append (THBaseServiceIf *iface, TResult ** _return, const GByteArray * table, const TAppend * tappend, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->append (iface, _return, table, tappend, io, error);
}

gboolean
t_h_base_service_if_open_scanner (THBaseServiceIf *iface, gint32* _return, const GByteArray * table, const TScan * tscan, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->open_scanner (iface, _return, table, tscan, io, error);
}

gboolean
t_h_base_service_if_get_scanner_rows (THBaseServiceIf *iface, GPtrArray ** _return, const gint32 scannerId, const gint32 numRows, TIOError ** io, TIllegalArgument ** ia, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get_scanner_rows (iface, _return, scannerId, numRows, io, ia, error);
}

gboolean
t_h_base_service_if_close_scanner (THBaseServiceIf *iface, const gint32 scannerId, TIOError ** io, TIllegalArgument ** ia, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->close_scanner (iface, scannerId, io, ia, error);
}

gboolean
t_h_base_service_if_mutate_row (THBaseServiceIf *iface, const GByteArray * table, const TRowMutations * trowMutations, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->mutate_row (iface, table, trowMutations, io, error);
}

gboolean
t_h_base_service_if_get_scanner_results (THBaseServiceIf *iface, GPtrArray ** _return, const GByteArray * table, const TScan * tscan, const gint32 numRows, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get_scanner_results (iface, _return, table, tscan, numRows, io, error);
}

gboolean
t_h_base_service_if_get_region_location (THBaseServiceIf *iface, THRegionLocation ** _return, const GByteArray * table, const GByteArray * row, const gboolean reload, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get_region_location (iface, _return, table, row, reload, io, error);
}

gboolean
t_h_base_service_if_get_all_region_locations (THBaseServiceIf *iface, GPtrArray ** _return, const GByteArray * table, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->get_all_region_locations (iface, _return, table, io, error);
}

gboolean
t_h_base_service_if_check_and_mutate (THBaseServiceIf *iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const TCompareOp compareOp, const GByteArray * value, const TRowMutations * rowMutations, TIOError ** io, GError **error)
{
  return T_H_BASE_SERVICE_IF_GET_INTERFACE (iface)->check_and_mutate (iface, _return, table, row, family, qualifier, compareOp, value, rowMutations, io, error);
}

GType
t_h_base_service_if_get_type (void)
{
  static GType type = 0;
  if (type == 0)
  {
    static const GTypeInfo type_info =
    {
      sizeof (THBaseServiceIfInterface),
      NULL,  /* base_init */
      NULL,  /* base_finalize */
      NULL,  /* class_init */
      NULL,  /* class_finalize */
      NULL,  /* class_data */
      0,     /* instance_size */
      0,     /* n_preallocs */
      NULL,  /* instance_init */
      NULL   /* value_table */
    };
    type = g_type_register_static (G_TYPE_INTERFACE,
                                   "THBaseServiceIf",
                                   &type_info, 0);
  }
  return type;
}

static void 
t_h_base_service_if_interface_init (THBaseServiceIfInterface *iface);

G_DEFINE_TYPE_WITH_CODE (THBaseServiceClient, t_h_base_service_client,
                         G_TYPE_OBJECT, 
                         G_IMPLEMENT_INTERFACE (TYPE_T_H_BASE_SERVICE_IF,
                                                t_h_base_service_if_interface_init))

enum _THBaseServiceClientProperties
{
  PROP_0,
  PROP_T_H_BASE_SERVICE_CLIENT_INPUT_PROTOCOL,
  PROP_T_H_BASE_SERVICE_CLIENT_OUTPUT_PROTOCOL
};

void
t_h_base_service_client_set_property (GObject *object, guint property_id, const GValue *value, GParamSpec *pspec)
{
  THBaseServiceClient *client = T_H_BASE_SERVICE_CLIENT (object);

  THRIFT_UNUSED_VAR (pspec);

  switch (property_id)
  {
    case PROP_T_H_BASE_SERVICE_CLIENT_INPUT_PROTOCOL:
      client->input_protocol = g_value_get_object (value);
      break;
    case PROP_T_H_BASE_SERVICE_CLIENT_OUTPUT_PROTOCOL:
      client->output_protocol = g_value_get_object (value);
      break;
  }
}

void
t_h_base_service_client_get_property (GObject *object, guint property_id, GValue *value, GParamSpec *pspec)
{
  THBaseServiceClient *client = T_H_BASE_SERVICE_CLIENT (object);

  THRIFT_UNUSED_VAR (pspec);

  switch (property_id)
  {
    case PROP_T_H_BASE_SERVICE_CLIENT_INPUT_PROTOCOL:
      g_value_set_object (value, client->input_protocol);
      break;
    case PROP_T_H_BASE_SERVICE_CLIENT_OUTPUT_PROTOCOL:
      g_value_set_object (value, client->output_protocol);
      break;
  }
}

gboolean t_h_base_service_client_send_exists (THBaseServiceIf * iface, const GByteArray * table, const TGet * tget, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "exists", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "exists_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tget", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tget), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_exists (THBaseServiceIf * iface, gboolean* _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "exists", 6) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected exists", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_BOOL)
          {
            if ((ret = thrift_protocol_read_bool (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_exists (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_exists (iface, table, tget, error))
    return FALSE;
  if (!t_h_base_service_client_recv_exists (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_exists_all (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tgets, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "existsAll", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "existsAll_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tgets", T_LIST, 2, error)) < 0)
      return 0;
    xfer += ret;
    {
      guint i72;

      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRUCT, (gint32) (tgets ? tgets->len : 0), error)) < 0)
        return 0;
      xfer += ret;
      for (i72 = 0; i72 < (tgets ? tgets->len : 0); i72++)
      {
        if ((ret = thrift_struct_write (THRIFT_STRUCT ((g_ptr_array_index ((GPtrArray *) tgets, i72))), protocol, error)) < 0)
          return 0;
        xfer += ret;

      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_exists_all (THBaseServiceIf * iface, GArray ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "existsAll", 9) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected existsAll", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                gboolean* _elem73 = g_new (gboolean, 1);
                if ((ret = thrift_protocol_read_bool (protocol, &*_elem73, error)) < 0)
                  return 0;
                xfer += ret;
                g_array_append_vals (*_return, _elem73, 1);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_exists_all (THBaseServiceIf * iface, GArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_exists_all (iface, table, tgets, error))
    return FALSE;
  if (!t_h_base_service_client_recv_exists_all (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get (THBaseServiceIf * iface, const GByteArray * table, const TGet * tget, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tget", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tget), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get (THBaseServiceIf * iface, TResult ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get", 3) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
            {
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_get (iface, table, tget, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get_multiple (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tgets, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "getMultiple", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "getMultiple_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tgets", T_LIST, 2, error)) < 0)
      return 0;
    xfer += ret;
    {
      guint i74;

      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRUCT, (gint32) (tgets ? tgets->len : 0), error)) < 0)
        return 0;
      xfer += ret;
      for (i74 = 0; i74 < (tgets ? tgets->len : 0); i74++)
      {
        if ((ret = thrift_struct_write (THRIFT_STRUCT ((g_ptr_array_index ((GPtrArray *) tgets, i74))), protocol, error)) < 0)
          return 0;
        xfer += ret;

      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get_multiple (THBaseServiceIf * iface, GPtrArray ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "getMultiple", 11) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected getMultiple", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                TResult * _elem75 = NULL;
                if ( _elem75 != NULL)
                {
                  g_object_unref (_elem75);
                }
                _elem75 = g_object_new (TYPE_T_RESULT, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem75), protocol, error)) < 0)
                {
                  g_object_unref (_elem75);
                  return 0;
                }
                xfer += ret;
                g_ptr_array_add (*_return, _elem75);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get_multiple (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_get_multiple (iface, table, tgets, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get_multiple (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_put (THBaseServiceIf * iface, const GByteArray * table, const TPut * tput, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "put", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "put_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tput", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tput), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_put (THBaseServiceIf * iface, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "put", 3) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected put", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_put (THBaseServiceIf * iface, const GByteArray * table, const TPut * tput, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_put (iface, table, tput, error))
    return FALSE;
  if (!t_h_base_service_client_recv_put (iface, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_check_and_put (THBaseServiceIf * iface, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TPut * tput, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "checkAndPut", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "checkAndPut_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "row", T_STRING, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, row ? ((GByteArray *) row)->data : NULL, row ? ((GByteArray *) row)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "family", T_STRING, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, family ? ((GByteArray *) family)->data : NULL, family ? ((GByteArray *) family)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "qualifier", T_STRING, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, qualifier ? ((GByteArray *) qualifier)->data : NULL, qualifier ? ((GByteArray *) qualifier)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "value", T_STRING, 5, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, value ? ((GByteArray *) value)->data : NULL, value ? ((GByteArray *) value)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tput", T_STRUCT, 6, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tput), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_check_and_put (THBaseServiceIf * iface, gboolean* _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "checkAndPut", 11) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected checkAndPut", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_BOOL)
          {
            if ((ret = thrift_protocol_read_bool (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_check_and_put (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TPut * tput, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_check_and_put (iface, table, row, family, qualifier, value, tput, error))
    return FALSE;
  if (!t_h_base_service_client_recv_check_and_put (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_put_multiple (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tputs, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "putMultiple", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "putMultiple_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tputs", T_LIST, 2, error)) < 0)
      return 0;
    xfer += ret;
    {
      guint i76;

      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRUCT, (gint32) (tputs ? tputs->len : 0), error)) < 0)
        return 0;
      xfer += ret;
      for (i76 = 0; i76 < (tputs ? tputs->len : 0); i76++)
      {
        if ((ret = thrift_struct_write (THRIFT_STRUCT ((g_ptr_array_index ((GPtrArray *) tputs, i76))), protocol, error)) < 0)
          return 0;
        xfer += ret;

      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_put_multiple (THBaseServiceIf * iface, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "putMultiple", 11) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected putMultiple", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_put_multiple (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tputs, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_put_multiple (iface, table, tputs, error))
    return FALSE;
  if (!t_h_base_service_client_recv_put_multiple (iface, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_delete_single (THBaseServiceIf * iface, const GByteArray * table, const TDelete * tdelete, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "deleteSingle", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "deleteSingle_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tdelete", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tdelete), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_delete_single (THBaseServiceIf * iface, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "deleteSingle", 12) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected deleteSingle", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_delete_single (THBaseServiceIf * iface, const GByteArray * table, const TDelete * tdelete, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_delete_single (iface, table, tdelete, error))
    return FALSE;
  if (!t_h_base_service_client_recv_delete_single (iface, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_delete_multiple (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tdeletes, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "deleteMultiple", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "deleteMultiple_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tdeletes", T_LIST, 2, error)) < 0)
      return 0;
    xfer += ret;
    {
      guint i77;

      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRUCT, (gint32) (tdeletes ? tdeletes->len : 0), error)) < 0)
        return 0;
      xfer += ret;
      for (i77 = 0; i77 < (tdeletes ? tdeletes->len : 0); i77++)
      {
        if ((ret = thrift_struct_write (THRIFT_STRUCT ((g_ptr_array_index ((GPtrArray *) tdeletes, i77))), protocol, error)) < 0)
          return 0;
        xfer += ret;

      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_delete_multiple (THBaseServiceIf * iface, GPtrArray ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "deleteMultiple", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected deleteMultiple", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                TDelete * _elem78 = NULL;
                if ( _elem78 != NULL)
                {
                  g_object_unref (_elem78);
                }
                _elem78 = g_object_new (TYPE_T_DELETE, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem78), protocol, error)) < 0)
                {
                  g_object_unref (_elem78);
                  return 0;
                }
                xfer += ret;
                g_ptr_array_add (*_return, _elem78);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_delete_multiple (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tdeletes, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_delete_multiple (iface, table, tdeletes, error))
    return FALSE;
  if (!t_h_base_service_client_recv_delete_multiple (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_check_and_delete (THBaseServiceIf * iface, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TDelete * tdelete, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "checkAndDelete", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "checkAndDelete_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "row", T_STRING, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, row ? ((GByteArray *) row)->data : NULL, row ? ((GByteArray *) row)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "family", T_STRING, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, family ? ((GByteArray *) family)->data : NULL, family ? ((GByteArray *) family)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "qualifier", T_STRING, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, qualifier ? ((GByteArray *) qualifier)->data : NULL, qualifier ? ((GByteArray *) qualifier)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "value", T_STRING, 5, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, value ? ((GByteArray *) value)->data : NULL, value ? ((GByteArray *) value)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tdelete", T_STRUCT, 6, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tdelete), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_check_and_delete (THBaseServiceIf * iface, gboolean* _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "checkAndDelete", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected checkAndDelete", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_BOOL)
          {
            if ((ret = thrift_protocol_read_bool (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_check_and_delete (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TDelete * tdelete, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_check_and_delete (iface, table, row, family, qualifier, value, tdelete, error))
    return FALSE;
  if (!t_h_base_service_client_recv_check_and_delete (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_increment (THBaseServiceIf * iface, const GByteArray * table, const TIncrement * tincrement, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "increment", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "increment_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tincrement", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tincrement), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_increment (THBaseServiceIf * iface, TResult ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "increment", 9) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected increment", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
            {
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_increment (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TIncrement * tincrement, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_increment (iface, table, tincrement, error))
    return FALSE;
  if (!t_h_base_service_client_recv_increment (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_append (THBaseServiceIf * iface, const GByteArray * table, const TAppend * tappend, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "append", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "append_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tappend", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tappend), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_append (THBaseServiceIf * iface, TResult ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "append", 6) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected append", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
            {
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_append (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TAppend * tappend, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_append (iface, table, tappend, error))
    return FALSE;
  if (!t_h_base_service_client_recv_append (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_open_scanner (THBaseServiceIf * iface, const GByteArray * table, const TScan * tscan, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "openScanner", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "openScanner_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tscan", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tscan), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_open_scanner (THBaseServiceIf * iface, gint32* _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "openScanner", 11) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected openScanner", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_I32)
          {
            if ((ret = thrift_protocol_read_i32 (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_open_scanner (THBaseServiceIf * iface, gint32* _return, const GByteArray * table, const TScan * tscan, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_open_scanner (iface, table, tscan, error))
    return FALSE;
  if (!t_h_base_service_client_recv_open_scanner (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get_scanner_rows (THBaseServiceIf * iface, const gint32 scannerId, const gint32 numRows, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "getScannerRows", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "getScannerRows_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "scannerId", T_I32, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, scannerId, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "numRows", T_I32, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, numRows, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get_scanner_rows (THBaseServiceIf * iface, GPtrArray ** _return, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "getScannerRows", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected getScannerRows", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                TResult * _elem79 = NULL;
                if ( _elem79 != NULL)
                {
                  g_object_unref (_elem79);
                }
                _elem79 = g_object_new (TYPE_T_RESULT, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem79), protocol, error)) < 0)
                {
                  g_object_unref (_elem79);
                  return 0;
                }
                xfer += ret;
                g_ptr_array_add (*_return, _elem79);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *ia != NULL)
            {
              g_object_unref (*ia);
            }
            *ia = g_object_new (TYPE_T_ILLEGAL_ARGUMENT, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ia), protocol, error)) < 0)
            {
              g_object_unref (*ia);
              *ia = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  if (*ia != NULL)
  {
      g_set_error (error, T_ILLEGAL_ARGUMENT_ERROR, T_ILLEGAL_ARGUMENT_ERROR_CODE, "TIllegalArgument");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get_scanner_rows (THBaseServiceIf * iface, GPtrArray ** _return, const gint32 scannerId, const gint32 numRows, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  if (!t_h_base_service_client_send_get_scanner_rows (iface, scannerId, numRows, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get_scanner_rows (iface, _return, io, ia, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_close_scanner (THBaseServiceIf * iface, const gint32 scannerId, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "closeScanner", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "closeScanner_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "scannerId", T_I32, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, scannerId, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_close_scanner (THBaseServiceIf * iface, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "closeScanner", 12) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected closeScanner", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *ia != NULL)
            {
              g_object_unref (*ia);
            }
            *ia = g_object_new (TYPE_T_ILLEGAL_ARGUMENT, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ia), protocol, error)) < 0)
            {
              g_object_unref (*ia);
              *ia = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  if (*ia != NULL)
  {
      g_set_error (error, T_ILLEGAL_ARGUMENT_ERROR, T_ILLEGAL_ARGUMENT_ERROR_CODE, "TIllegalArgument");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_close_scanner (THBaseServiceIf * iface, const gint32 scannerId, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  if (!t_h_base_service_client_send_close_scanner (iface, scannerId, error))
    return FALSE;
  if (!t_h_base_service_client_recv_close_scanner (iface, io, ia, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_mutate_row (THBaseServiceIf * iface, const GByteArray * table, const TRowMutations * trowMutations, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "mutateRow", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "mutateRow_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "trowMutations", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (trowMutations), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_mutate_row (THBaseServiceIf * iface, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "mutateRow", 9) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected mutateRow", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_mutate_row (THBaseServiceIf * iface, const GByteArray * table, const TRowMutations * trowMutations, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_mutate_row (iface, table, trowMutations, error))
    return FALSE;
  if (!t_h_base_service_client_recv_mutate_row (iface, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get_scanner_results (THBaseServiceIf * iface, const GByteArray * table, const TScan * tscan, const gint32 numRows, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "getScannerResults", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "getScannerResults_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "tscan", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (tscan), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "numRows", T_I32, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, numRows, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get_scanner_results (THBaseServiceIf * iface, GPtrArray ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "getScannerResults", 17) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected getScannerResults", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                TResult * _elem80 = NULL;
                if ( _elem80 != NULL)
                {
                  g_object_unref (_elem80);
                }
                _elem80 = g_object_new (TYPE_T_RESULT, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem80), protocol, error)) < 0)
                {
                  g_object_unref (_elem80);
                  return 0;
                }
                xfer += ret;
                g_ptr_array_add (*_return, _elem80);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get_scanner_results (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const TScan * tscan, const gint32 numRows, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_get_scanner_results (iface, table, tscan, numRows, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get_scanner_results (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get_region_location (THBaseServiceIf * iface, const GByteArray * table, const GByteArray * row, const gboolean reload, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "getRegionLocation", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "getRegionLocation_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "row", T_STRING, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, row ? ((GByteArray *) row)->data : NULL, row ? ((GByteArray *) row)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "reload", T_BOOL, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_bool (protocol, reload, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get_region_location (THBaseServiceIf * iface, THRegionLocation ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "getRegionLocation", 17) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected getRegionLocation", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
            {
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get_region_location (THBaseServiceIf * iface, THRegionLocation ** _return, const GByteArray * table, const GByteArray * row, const gboolean reload, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_get_region_location (iface, table, row, reload, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get_region_location (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_get_all_region_locations (THBaseServiceIf * iface, const GByteArray * table, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "getAllRegionLocations", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "getAllRegionLocations_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_get_all_region_locations (THBaseServiceIf * iface, GPtrArray ** _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "getAllRegionLocations", 21) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected getAllRegionLocations", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              guint32 i;
              ThriftType element_type;

              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type,&size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              for (i = 0; i < size; i++)
              {
                THRegionLocation * _elem81 = NULL;
                if ( _elem81 != NULL)
                {
                  g_object_unref (_elem81);
                }
                _elem81 = g_object_new (TYPE_T_H_REGION_LOCATION, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem81), protocol, error)) < 0)
                {
                  g_object_unref (_elem81);
                  return 0;
                }
                xfer += ret;
                g_ptr_array_add (*_return, _elem81);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_get_all_region_locations (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_get_all_region_locations (iface, table, error))
    return FALSE;
  if (!t_h_base_service_client_recv_get_all_region_locations (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

gboolean t_h_base_service_client_send_check_and_mutate (THBaseServiceIf * iface, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const TCompareOp compareOp, const GByteArray * value, const TRowMutations * rowMutations, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "checkAndMutate", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "checkAndMutate_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "table", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, table ? ((GByteArray *) table)->data : NULL, table ? ((GByteArray *) table)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "row", T_STRING, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, row ? ((GByteArray *) row)->data : NULL, row ? ((GByteArray *) row)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "family", T_STRING, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, family ? ((GByteArray *) family)->data : NULL, family ? ((GByteArray *) family)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "qualifier", T_STRING, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, qualifier ? ((GByteArray *) qualifier)->data : NULL, qualifier ? ((GByteArray *) qualifier)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "compareOp", T_I32, 5, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) compareOp, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "value", T_STRING, 6, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, value ? ((GByteArray *) value)->data : NULL, value ? ((GByteArray *) value)->len : 0, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "rowMutations", T_STRUCT, 7, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (rowMutations), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean t_h_base_service_client_recv_check_and_mutate (THBaseServiceIf * iface, gboolean* _return, TIOError ** io, GError ** error)
{
  gint32 rseqid;
  gchar * fname = NULL;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = T_H_BASE_SERVICE_CLIENT (iface)->input_protocol;
  ThriftApplicationException *xception;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0) {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR,xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "checkAndMutate", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol,error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected checkAndMutate", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name = NULL;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);
    name = NULL;

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);
      name = NULL;

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_BOOL)
          {
            if ((ret = thrift_protocol_read_bool (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            /* This struct is an exception */
            if ( *io != NULL)
            {
              g_object_unref (*io);
            }
            *io = g_object_new (TYPE_T_I_O_ERROR, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*io), protocol, error)) < 0)
            {
              g_object_unref (*io);
              *io = NULL;
              return 0;
            }
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*io != NULL)
  {
      g_set_error (error, T_I_O_ERROR_ERROR, T_I_O_ERROR_ERROR_CODE, "TIOError");
      return FALSE;
  }
  return TRUE;
}

gboolean t_h_base_service_client_check_and_mutate (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const TCompareOp compareOp, const GByteArray * value, const TRowMutations * rowMutations, TIOError ** io, GError ** error)
{
  if (!t_h_base_service_client_send_check_and_mutate (iface, table, row, family, qualifier, compareOp, value, rowMutations, error))
    return FALSE;
  if (!t_h_base_service_client_recv_check_and_mutate (iface, _return, io, error))
    return FALSE;
  return TRUE;
}

static void
t_h_base_service_if_interface_init (THBaseServiceIfInterface *iface)
{
  iface->exists = t_h_base_service_client_exists;
  iface->exists_all = t_h_base_service_client_exists_all;
  iface->get = t_h_base_service_client_get;
  iface->get_multiple = t_h_base_service_client_get_multiple;
  iface->put = t_h_base_service_client_put;
  iface->check_and_put = t_h_base_service_client_check_and_put;
  iface->put_multiple = t_h_base_service_client_put_multiple;
  iface->delete_single = t_h_base_service_client_delete_single;
  iface->delete_multiple = t_h_base_service_client_delete_multiple;
  iface->check_and_delete = t_h_base_service_client_check_and_delete;
  iface->increment = t_h_base_service_client_increment;
  iface->append = t_h_base_service_client_append;
  iface->open_scanner = t_h_base_service_client_open_scanner;
  iface->get_scanner_rows = t_h_base_service_client_get_scanner_rows;
  iface->close_scanner = t_h_base_service_client_close_scanner;
  iface->mutate_row = t_h_base_service_client_mutate_row;
  iface->get_scanner_results = t_h_base_service_client_get_scanner_results;
  iface->get_region_location = t_h_base_service_client_get_region_location;
  iface->get_all_region_locations = t_h_base_service_client_get_all_region_locations;
  iface->check_and_mutate = t_h_base_service_client_check_and_mutate;
}

static void
t_h_base_service_client_init (THBaseServiceClient *client)
{
  client->input_protocol = NULL;
  client->output_protocol = NULL;
}

static void
t_h_base_service_client_class_init (THBaseServiceClientClass *cls)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (cls);
  GParamSpec *param_spec;

  gobject_class->set_property = t_h_base_service_client_set_property;
  gobject_class->get_property = t_h_base_service_client_get_property;

  param_spec = g_param_spec_object ("input_protocol",
                                    "input protocol (construct)",
                                    "Set the client input protocol",
                                    THRIFT_TYPE_PROTOCOL,
                                    G_PARAM_READWRITE);
  g_object_class_install_property (gobject_class,
                                   PROP_T_H_BASE_SERVICE_CLIENT_INPUT_PROTOCOL, param_spec);

  param_spec = g_param_spec_object ("output_protocol",
                                    "output protocol (construct)",
                                    "Set the client output protocol",
                                    THRIFT_TYPE_PROTOCOL,
                                    G_PARAM_READWRITE);
  g_object_class_install_property (gobject_class,
                                   PROP_T_H_BASE_SERVICE_CLIENT_OUTPUT_PROTOCOL, param_spec);
}

static void
t_h_base_service_handler_t_h_base_service_if_interface_init (THBaseServiceIfInterface *iface);

G_DEFINE_TYPE_WITH_CODE (THBaseServiceHandler, 
                         t_h_base_service_handler,
                         G_TYPE_OBJECT,
                         G_IMPLEMENT_INTERFACE (TYPE_T_H_BASE_SERVICE_IF,
                                                t_h_base_service_handler_t_h_base_service_if_interface_init))

gboolean t_h_base_service_handler_exists (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->exists (iface, _return, table, tget, io, error);
}

gboolean t_h_base_service_handler_exists_all (THBaseServiceIf * iface, GArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->exists_all (iface, _return, table, tgets, io, error);
}

gboolean t_h_base_service_handler_get (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TGet * tget, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get (iface, _return, table, tget, io, error);
}

gboolean t_h_base_service_handler_get_multiple (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tgets, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get_multiple (iface, _return, table, tgets, io, error);
}

gboolean t_h_base_service_handler_put (THBaseServiceIf * iface, const GByteArray * table, const TPut * tput, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->put (iface, table, tput, io, error);
}

gboolean t_h_base_service_handler_check_and_put (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TPut * tput, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->check_and_put (iface, _return, table, row, family, qualifier, value, tput, io, error);
}

gboolean t_h_base_service_handler_put_multiple (THBaseServiceIf * iface, const GByteArray * table, const GPtrArray * tputs, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->put_multiple (iface, table, tputs, io, error);
}

gboolean t_h_base_service_handler_delete_single (THBaseServiceIf * iface, const GByteArray * table, const TDelete * tdelete, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->delete_single (iface, table, tdelete, io, error);
}

gboolean t_h_base_service_handler_delete_multiple (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const GPtrArray * tdeletes, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->delete_multiple (iface, _return, table, tdeletes, io, error);
}

gboolean t_h_base_service_handler_check_and_delete (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const GByteArray * value, const TDelete * tdelete, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->check_and_delete (iface, _return, table, row, family, qualifier, value, tdelete, io, error);
}

gboolean t_h_base_service_handler_increment (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TIncrement * tincrement, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->increment (iface, _return, table, tincrement, io, error);
}

gboolean t_h_base_service_handler_append (THBaseServiceIf * iface, TResult ** _return, const GByteArray * table, const TAppend * tappend, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->append (iface, _return, table, tappend, io, error);
}

gboolean t_h_base_service_handler_open_scanner (THBaseServiceIf * iface, gint32* _return, const GByteArray * table, const TScan * tscan, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->open_scanner (iface, _return, table, tscan, io, error);
}

gboolean t_h_base_service_handler_get_scanner_rows (THBaseServiceIf * iface, GPtrArray ** _return, const gint32 scannerId, const gint32 numRows, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get_scanner_rows (iface, _return, scannerId, numRows, io, ia, error);
}

gboolean t_h_base_service_handler_close_scanner (THBaseServiceIf * iface, const gint32 scannerId, TIOError ** io, TIllegalArgument ** ia, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->close_scanner (iface, scannerId, io, ia, error);
}

gboolean t_h_base_service_handler_mutate_row (THBaseServiceIf * iface, const GByteArray * table, const TRowMutations * trowMutations, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->mutate_row (iface, table, trowMutations, io, error);
}

gboolean t_h_base_service_handler_get_scanner_results (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, const TScan * tscan, const gint32 numRows, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get_scanner_results (iface, _return, table, tscan, numRows, io, error);
}

gboolean t_h_base_service_handler_get_region_location (THBaseServiceIf * iface, THRegionLocation ** _return, const GByteArray * table, const GByteArray * row, const gboolean reload, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get_region_location (iface, _return, table, row, reload, io, error);
}

gboolean t_h_base_service_handler_get_all_region_locations (THBaseServiceIf * iface, GPtrArray ** _return, const GByteArray * table, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->get_all_region_locations (iface, _return, table, io, error);
}

gboolean t_h_base_service_handler_check_and_mutate (THBaseServiceIf * iface, gboolean* _return, const GByteArray * table, const GByteArray * row, const GByteArray * family, const GByteArray * qualifier, const TCompareOp compareOp, const GByteArray * value, const TRowMutations * rowMutations, TIOError ** io, GError ** error)
{
  g_return_val_if_fail (IS_T_H_BASE_SERVICE_HANDLER (iface), FALSE);

  return T_H_BASE_SERVICE_HANDLER_GET_CLASS (iface)->check_and_mutate (iface, _return, table, row, family, qualifier, compareOp, value, rowMutations, io, error);
}

static void
t_h_base_service_handler_t_h_base_service_if_interface_init (THBaseServiceIfInterface *iface)
{
  iface->exists = t_h_base_service_handler_exists;
  iface->exists_all = t_h_base_service_handler_exists_all;
  iface->get = t_h_base_service_handler_get;
  iface->get_multiple = t_h_base_service_handler_get_multiple;
  iface->put = t_h_base_service_handler_put;
  iface->check_and_put = t_h_base_service_handler_check_and_put;
  iface->put_multiple = t_h_base_service_handler_put_multiple;
  iface->delete_single = t_h_base_service_handler_delete_single;
  iface->delete_multiple = t_h_base_service_handler_delete_multiple;
  iface->check_and_delete = t_h_base_service_handler_check_and_delete;
  iface->increment = t_h_base_service_handler_increment;
  iface->append = t_h_base_service_handler_append;
  iface->open_scanner = t_h_base_service_handler_open_scanner;
  iface->get_scanner_rows = t_h_base_service_handler_get_scanner_rows;
  iface->close_scanner = t_h_base_service_handler_close_scanner;
  iface->mutate_row = t_h_base_service_handler_mutate_row;
  iface->get_scanner_results = t_h_base_service_handler_get_scanner_results;
  iface->get_region_location = t_h_base_service_handler_get_region_location;
  iface->get_all_region_locations = t_h_base_service_handler_get_all_region_locations;
  iface->check_and_mutate = t_h_base_service_handler_check_and_mutate;
}

static void
t_h_base_service_handler_init (THBaseServiceHandler *self)
{
  THRIFT_UNUSED_VAR (self);
}

static void
t_h_base_service_handler_class_init (THBaseServiceHandlerClass *cls)
{
  cls->exists = NULL;
  cls->exists_all = NULL;
  cls->get = NULL;
  cls->get_multiple = NULL;
  cls->put = NULL;
  cls->check_and_put = NULL;
  cls->put_multiple = NULL;
  cls->delete_single = NULL;
  cls->delete_multiple = NULL;
  cls->check_and_delete = NULL;
  cls->increment = NULL;
  cls->append = NULL;
  cls->open_scanner = NULL;
  cls->get_scanner_rows = NULL;
  cls->close_scanner = NULL;
  cls->mutate_row = NULL;
  cls->get_scanner_results = NULL;
  cls->get_region_location = NULL;
  cls->get_all_region_locations = NULL;
  cls->check_and_mutate = NULL;
}

enum _THBaseServiceProcessorProperties
{
  PROP_T_H_BASE_SERVICE_PROCESSOR_0,
  PROP_T_H_BASE_SERVICE_PROCESSOR_HANDLER
};

G_DEFINE_TYPE (THBaseServiceProcessor,
               t_h_base_service_processor,
               THRIFT_TYPE_DISPATCH_PROCESSOR)

typedef gboolean (* THBaseServiceProcessorProcessFunction) (THBaseServiceProcessor *, 
                                                            gint32,
                                                            ThriftProtocol *,
                                                            ThriftProtocol *,
                                                            GError **);

typedef struct
{
  gchar *name;
  THBaseServiceProcessorProcessFunction function;
} t_h_base_service_processor_process_function_def;

static gboolean
t_h_base_service_processor_process_exists (THBaseServiceProcessor *,
                                           gint32,
                                           ThriftProtocol *,
                                           ThriftProtocol *,
                                           GError **);
static gboolean
t_h_base_service_processor_process_exists_all (THBaseServiceProcessor *,
                                               gint32,
                                               ThriftProtocol *,
                                               ThriftProtocol *,
                                               GError **);
static gboolean
t_h_base_service_processor_process_get (THBaseServiceProcessor *,
                                        gint32,
                                        ThriftProtocol *,
                                        ThriftProtocol *,
                                        GError **);
static gboolean
t_h_base_service_processor_process_get_multiple (THBaseServiceProcessor *,
                                                 gint32,
                                                 ThriftProtocol *,
                                                 ThriftProtocol *,
                                                 GError **);
static gboolean
t_h_base_service_processor_process_put (THBaseServiceProcessor *,
                                        gint32,
                                        ThriftProtocol *,
                                        ThriftProtocol *,
                                        GError **);
static gboolean
t_h_base_service_processor_process_check_and_put (THBaseServiceProcessor *,
                                                  gint32,
                                                  ThriftProtocol *,
                                                  ThriftProtocol *,
                                                  GError **);
static gboolean
t_h_base_service_processor_process_put_multiple (THBaseServiceProcessor *,
                                                 gint32,
                                                 ThriftProtocol *,
                                                 ThriftProtocol *,
                                                 GError **);
static gboolean
t_h_base_service_processor_process_delete_single (THBaseServiceProcessor *,
                                                  gint32,
                                                  ThriftProtocol *,
                                                  ThriftProtocol *,
                                                  GError **);
static gboolean
t_h_base_service_processor_process_delete_multiple (THBaseServiceProcessor *,
                                                    gint32,
                                                    ThriftProtocol *,
                                                    ThriftProtocol *,
                                                    GError **);
static gboolean
t_h_base_service_processor_process_check_and_delete (THBaseServiceProcessor *,
                                                     gint32,
                                                     ThriftProtocol *,
                                                     ThriftProtocol *,
                                                     GError **);
static gboolean
t_h_base_service_processor_process_increment (THBaseServiceProcessor *,
                                              gint32,
                                              ThriftProtocol *,
                                              ThriftProtocol *,
                                              GError **);
static gboolean
t_h_base_service_processor_process_append (THBaseServiceProcessor *,
                                           gint32,
                                           ThriftProtocol *,
                                           ThriftProtocol *,
                                           GError **);
static gboolean
t_h_base_service_processor_process_open_scanner (THBaseServiceProcessor *,
                                                 gint32,
                                                 ThriftProtocol *,
                                                 ThriftProtocol *,
                                                 GError **);
static gboolean
t_h_base_service_processor_process_get_scanner_rows (THBaseServiceProcessor *,
                                                     gint32,
                                                     ThriftProtocol *,
                                                     ThriftProtocol *,
                                                     GError **);
static gboolean
t_h_base_service_processor_process_close_scanner (THBaseServiceProcessor *,
                                                  gint32,
                                                  ThriftProtocol *,
                                                  ThriftProtocol *,
                                                  GError **);
static gboolean
t_h_base_service_processor_process_mutate_row (THBaseServiceProcessor *,
                                               gint32,
                                               ThriftProtocol *,
                                               ThriftProtocol *,
                                               GError **);
static gboolean
t_h_base_service_processor_process_get_scanner_results (THBaseServiceProcessor *,
                                                        gint32,
                                                        ThriftProtocol *,
                                                        ThriftProtocol *,
                                                        GError **);
static gboolean
t_h_base_service_processor_process_get_region_location (THBaseServiceProcessor *,
                                                        gint32,
                                                        ThriftProtocol *,
                                                        ThriftProtocol *,
                                                        GError **);
static gboolean
t_h_base_service_processor_process_get_all_region_locations (THBaseServiceProcessor *,
                                                             gint32,
                                                             ThriftProtocol *,
                                                             ThriftProtocol *,
                                                             GError **);
static gboolean
t_h_base_service_processor_process_check_and_mutate (THBaseServiceProcessor *,
                                                     gint32,
                                                     ThriftProtocol *,
                                                     ThriftProtocol *,
                                                     GError **);

static t_h_base_service_processor_process_function_def
t_h_base_service_processor_process_function_defs[20] = {
  {
    "exists",
    t_h_base_service_processor_process_exists
  },
  {
    "existsAll",
    t_h_base_service_processor_process_exists_all
  },
  {
    "get",
    t_h_base_service_processor_process_get
  },
  {
    "getMultiple",
    t_h_base_service_processor_process_get_multiple
  },
  {
    "put",
    t_h_base_service_processor_process_put
  },
  {
    "checkAndPut",
    t_h_base_service_processor_process_check_and_put
  },
  {
    "putMultiple",
    t_h_base_service_processor_process_put_multiple
  },
  {
    "deleteSingle",
    t_h_base_service_processor_process_delete_single
  },
  {
    "deleteMultiple",
    t_h_base_service_processor_process_delete_multiple
  },
  {
    "checkAndDelete",
    t_h_base_service_processor_process_check_and_delete
  },
  {
    "increment",
    t_h_base_service_processor_process_increment
  },
  {
    "append",
    t_h_base_service_processor_process_append
  },
  {
    "openScanner",
    t_h_base_service_processor_process_open_scanner
  },
  {
    "getScannerRows",
    t_h_base_service_processor_process_get_scanner_rows
  },
  {
    "closeScanner",
    t_h_base_service_processor_process_close_scanner
  },
  {
    "mutateRow",
    t_h_base_service_processor_process_mutate_row
  },
  {
    "getScannerResults",
    t_h_base_service_processor_process_get_scanner_results
  },
  {
    "getRegionLocation",
    t_h_base_service_processor_process_get_region_location
  },
  {
    "getAllRegionLocations",
    t_h_base_service_processor_process_get_all_region_locations
  },
  {
    "checkAndMutate",
    t_h_base_service_processor_process_check_and_mutate
  }
};

static gboolean
t_h_base_service_processor_process_exists (THBaseServiceProcessor *self,
                                           gint32 sequence_id,
                                           ThriftProtocol *input_protocol,
                                           ThriftProtocol *output_protocol,
                                           GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceExistsArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_EXISTS_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TGet * tget;
    TIOError * io = NULL;
    gboolean return_value;
    THBaseServiceExistsResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tget", &tget,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_EXISTS_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_exists (T_H_BASE_SERVICE_IF (self->handler),
                                         &return_value,
                                         table,
                                         tget,
                                         &io,
                                         error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "exists",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "exists",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.exists implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "exists",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tget != NULL)
      g_object_unref (tget);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_exists_all (THBaseServiceProcessor *self,
                                               gint32 sequence_id,
                                               ThriftProtocol *input_protocol,
                                               ThriftProtocol *output_protocol,
                                               GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceExistsAllArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_EXISTS_ALL_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GPtrArray * tgets;
    TIOError * io = NULL;
    GArray * return_value;
    THBaseServiceExistsAllResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tgets", &tgets,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_EXISTS_ALL_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_exists_all (T_H_BASE_SERVICE_IF (self->handler),
                                             &return_value,
                                             table,
                                             tgets,
                                             &io,
                                             error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "existsAll",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "existsAll",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.existsAll implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "existsAll",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tgets != NULL)
      g_ptr_array_unref (tgets);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get (THBaseServiceProcessor *self,
                                        gint32 sequence_id,
                                        ThriftProtocol *input_protocol,
                                        ThriftProtocol *output_protocol,
                                        GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TGet * tget;
    TIOError * io = NULL;
    TResult * return_value;
    THBaseServiceGetResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tget", &tget,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get (T_H_BASE_SERVICE_IF (self->handler),
                                      &return_value,
                                      table,
                                      tget,
                                      &io,
                                      error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_object_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "get",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "get",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.get implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "get",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tget != NULL)
      g_object_unref (tget);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get_multiple (THBaseServiceProcessor *self,
                                                 gint32 sequence_id,
                                                 ThriftProtocol *input_protocol,
                                                 ThriftProtocol *output_protocol,
                                                 GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetMultipleArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_MULTIPLE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GPtrArray * tgets;
    TIOError * io = NULL;
    GPtrArray * return_value;
    THBaseServiceGetMultipleResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tgets", &tgets,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_MULTIPLE_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get_multiple (T_H_BASE_SERVICE_IF (self->handler),
                                               &return_value,
                                               table,
                                               tgets,
                                               &io,
                                               error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_ptr_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "getMultiple",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getMultiple",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.getMultiple implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getMultiple",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tgets != NULL)
      g_ptr_array_unref (tgets);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_put (THBaseServiceProcessor *self,
                                        gint32 sequence_id,
                                        ThriftProtocol *input_protocol,
                                        ThriftProtocol *output_protocol,
                                        GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServicePutArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_PUT_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TPut * tput;
    TIOError * io = NULL;
    THBaseServicePutResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tput", &tput,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_PUT_RESULT, NULL);

    if (t_h_base_service_handler_put (T_H_BASE_SERVICE_IF (self->handler),
                                      table,
                                      tput,
                                      &io,
                                      error) == TRUE)
    {
      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "put",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "put",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.put implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "put",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tput != NULL)
      g_object_unref (tput);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_check_and_put (THBaseServiceProcessor *self,
                                                  gint32 sequence_id,
                                                  ThriftProtocol *input_protocol,
                                                  ThriftProtocol *output_protocol,
                                                  GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceCheckAndPutArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_PUT_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GByteArray * row;
    GByteArray * family;
    GByteArray * qualifier;
    GByteArray * value;
    TPut * tput;
    TIOError * io = NULL;
    gboolean return_value;
    THBaseServiceCheckAndPutResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "row", &row,
                  "family", &family,
                  "qualifier", &qualifier,
                  "value", &value,
                  "tput", &tput,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_PUT_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_check_and_put (T_H_BASE_SERVICE_IF (self->handler),
                                                &return_value,
                                                table,
                                                row,
                                                family,
                                                qualifier,
                                                value,
                                                tput,
                                                &io,
                                                error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "checkAndPut",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndPut",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.checkAndPut implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndPut",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (row != NULL)
      g_byte_array_unref (row);
    if (family != NULL)
      g_byte_array_unref (family);
    if (qualifier != NULL)
      g_byte_array_unref (qualifier);
    if (value != NULL)
      g_byte_array_unref (value);
    if (tput != NULL)
      g_object_unref (tput);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_put_multiple (THBaseServiceProcessor *self,
                                                 gint32 sequence_id,
                                                 ThriftProtocol *input_protocol,
                                                 ThriftProtocol *output_protocol,
                                                 GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServicePutMultipleArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_PUT_MULTIPLE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GPtrArray * tputs;
    TIOError * io = NULL;
    THBaseServicePutMultipleResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tputs", &tputs,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_PUT_MULTIPLE_RESULT, NULL);

    if (t_h_base_service_handler_put_multiple (T_H_BASE_SERVICE_IF (self->handler),
                                               table,
                                               tputs,
                                               &io,
                                               error) == TRUE)
    {
      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "putMultiple",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "putMultiple",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.putMultiple implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "putMultiple",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tputs != NULL)
      g_ptr_array_unref (tputs);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_delete_single (THBaseServiceProcessor *self,
                                                  gint32 sequence_id,
                                                  ThriftProtocol *input_protocol,
                                                  ThriftProtocol *output_protocol,
                                                  GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceDeleteSingleArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_DELETE_SINGLE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TDelete * tdelete;
    TIOError * io = NULL;
    THBaseServiceDeleteSingleResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tdelete", &tdelete,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_DELETE_SINGLE_RESULT, NULL);

    if (t_h_base_service_handler_delete_single (T_H_BASE_SERVICE_IF (self->handler),
                                                table,
                                                tdelete,
                                                &io,
                                                error) == TRUE)
    {
      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "deleteSingle",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "deleteSingle",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.deleteSingle implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "deleteSingle",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tdelete != NULL)
      g_object_unref (tdelete);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_delete_multiple (THBaseServiceProcessor *self,
                                                    gint32 sequence_id,
                                                    ThriftProtocol *input_protocol,
                                                    ThriftProtocol *output_protocol,
                                                    GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceDeleteMultipleArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_DELETE_MULTIPLE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GPtrArray * tdeletes;
    TIOError * io = NULL;
    GPtrArray * return_value;
    THBaseServiceDeleteMultipleResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tdeletes", &tdeletes,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_DELETE_MULTIPLE_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_delete_multiple (T_H_BASE_SERVICE_IF (self->handler),
                                                  &return_value,
                                                  table,
                                                  tdeletes,
                                                  &io,
                                                  error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_ptr_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "deleteMultiple",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "deleteMultiple",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.deleteMultiple implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "deleteMultiple",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tdeletes != NULL)
      g_ptr_array_unref (tdeletes);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_check_and_delete (THBaseServiceProcessor *self,
                                                     gint32 sequence_id,
                                                     ThriftProtocol *input_protocol,
                                                     ThriftProtocol *output_protocol,
                                                     GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceCheckAndDeleteArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_DELETE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GByteArray * row;
    GByteArray * family;
    GByteArray * qualifier;
    GByteArray * value;
    TDelete * tdelete;
    TIOError * io = NULL;
    gboolean return_value;
    THBaseServiceCheckAndDeleteResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "row", &row,
                  "family", &family,
                  "qualifier", &qualifier,
                  "value", &value,
                  "tdelete", &tdelete,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_DELETE_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_check_and_delete (T_H_BASE_SERVICE_IF (self->handler),
                                                   &return_value,
                                                   table,
                                                   row,
                                                   family,
                                                   qualifier,
                                                   value,
                                                   tdelete,
                                                   &io,
                                                   error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "checkAndDelete",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndDelete",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.checkAndDelete implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndDelete",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (row != NULL)
      g_byte_array_unref (row);
    if (family != NULL)
      g_byte_array_unref (family);
    if (qualifier != NULL)
      g_byte_array_unref (qualifier);
    if (value != NULL)
      g_byte_array_unref (value);
    if (tdelete != NULL)
      g_object_unref (tdelete);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_increment (THBaseServiceProcessor *self,
                                              gint32 sequence_id,
                                              ThriftProtocol *input_protocol,
                                              ThriftProtocol *output_protocol,
                                              GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceIncrementArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_INCREMENT_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TIncrement * tincrement;
    TIOError * io = NULL;
    TResult * return_value;
    THBaseServiceIncrementResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tincrement", &tincrement,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_INCREMENT_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_increment (T_H_BASE_SERVICE_IF (self->handler),
                                            &return_value,
                                            table,
                                            tincrement,
                                            &io,
                                            error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_object_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "increment",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "increment",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.increment implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "increment",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tincrement != NULL)
      g_object_unref (tincrement);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_append (THBaseServiceProcessor *self,
                                           gint32 sequence_id,
                                           ThriftProtocol *input_protocol,
                                           ThriftProtocol *output_protocol,
                                           GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceAppendArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_APPEND_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TAppend * tappend;
    TIOError * io = NULL;
    TResult * return_value;
    THBaseServiceAppendResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tappend", &tappend,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_APPEND_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_append (T_H_BASE_SERVICE_IF (self->handler),
                                         &return_value,
                                         table,
                                         tappend,
                                         &io,
                                         error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_object_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "append",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "append",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.append implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "append",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tappend != NULL)
      g_object_unref (tappend);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_open_scanner (THBaseServiceProcessor *self,
                                                 gint32 sequence_id,
                                                 ThriftProtocol *input_protocol,
                                                 ThriftProtocol *output_protocol,
                                                 GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceOpenScannerArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_OPEN_SCANNER_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TScan * tscan;
    TIOError * io = NULL;
    gint return_value;
    THBaseServiceOpenScannerResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tscan", &tscan,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_OPEN_SCANNER_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_open_scanner (T_H_BASE_SERVICE_IF (self->handler),
                                               (gint32 *)&return_value,
                                               table,
                                               tscan,
                                               &io,
                                               error) == TRUE)
    {
      g_object_set (result_struct, "success", (gint)(gint32)return_value, NULL);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "openScanner",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "openScanner",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.openScanner implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "openScanner",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tscan != NULL)
      g_object_unref (tscan);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get_scanner_rows (THBaseServiceProcessor *self,
                                                     gint32 sequence_id,
                                                     ThriftProtocol *input_protocol,
                                                     ThriftProtocol *output_protocol,
                                                     GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetScannerRowsArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_SCANNER_ROWS_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    gint scannerId;
    gint numRows;
    TIOError * io = NULL;
    TIllegalArgument * ia = NULL;
    GPtrArray * return_value;
    THBaseServiceGetScannerRowsResult * result_struct;

    g_object_get (args,
                  "scannerId", &scannerId,
                  "numRows", &numRows,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_SCANNER_ROWS_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get_scanner_rows (T_H_BASE_SERVICE_IF (self->handler),
                                                   &return_value,
                                                   scannerId,
                                                   numRows,
                                                   &io,
                                                   &ia,
                                                   error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_ptr_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "getScannerRows",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getScannerRows",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
if (ia != NULL)
      {
        g_object_set (result_struct,
                      "ia", ia,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getScannerRows",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.getScannerRows implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getScannerRows",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_close_scanner (THBaseServiceProcessor *self,
                                                  gint32 sequence_id,
                                                  ThriftProtocol *input_protocol,
                                                  ThriftProtocol *output_protocol,
                                                  GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceCloseScannerArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_CLOSE_SCANNER_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    gint scannerId;
    TIOError * io = NULL;
    TIllegalArgument * ia = NULL;
    THBaseServiceCloseScannerResult * result_struct;

    g_object_get (args,
                  "scannerId", &scannerId,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_CLOSE_SCANNER_RESULT, NULL);

    if (t_h_base_service_handler_close_scanner (T_H_BASE_SERVICE_IF (self->handler),
                                                scannerId,
                                                &io,
                                                &ia,
                                                error) == TRUE)
    {
      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "closeScanner",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "closeScanner",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
if (ia != NULL)
      {
        g_object_set (result_struct,
                      "ia", ia,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "closeScanner",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.closeScanner implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "closeScanner",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_mutate_row (THBaseServiceProcessor *self,
                                               gint32 sequence_id,
                                               ThriftProtocol *input_protocol,
                                               ThriftProtocol *output_protocol,
                                               GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceMutateRowArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_MUTATE_ROW_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TRowMutations * trowMutations;
    TIOError * io = NULL;
    THBaseServiceMutateRowResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "trowMutations", &trowMutations,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_MUTATE_ROW_RESULT, NULL);

    if (t_h_base_service_handler_mutate_row (T_H_BASE_SERVICE_IF (self->handler),
                                             table,
                                             trowMutations,
                                             &io,
                                             error) == TRUE)
    {
      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "mutateRow",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "mutateRow",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.mutateRow implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "mutateRow",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (trowMutations != NULL)
      g_object_unref (trowMutations);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get_scanner_results (THBaseServiceProcessor *self,
                                                        gint32 sequence_id,
                                                        ThriftProtocol *input_protocol,
                                                        ThriftProtocol *output_protocol,
                                                        GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetScannerResultsArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_SCANNER_RESULTS_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TScan * tscan;
    gint numRows;
    TIOError * io = NULL;
    GPtrArray * return_value;
    THBaseServiceGetScannerResultsResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "tscan", &tscan,
                  "numRows", &numRows,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_SCANNER_RESULTS_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get_scanner_results (T_H_BASE_SERVICE_IF (self->handler),
                                                      &return_value,
                                                      table,
                                                      tscan,
                                                      numRows,
                                                      &io,
                                                      error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_ptr_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "getScannerResults",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getScannerResults",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.getScannerResults implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getScannerResults",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (tscan != NULL)
      g_object_unref (tscan);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get_region_location (THBaseServiceProcessor *self,
                                                        gint32 sequence_id,
                                                        ThriftProtocol *input_protocol,
                                                        ThriftProtocol *output_protocol,
                                                        GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetRegionLocationArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_REGION_LOCATION_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GByteArray * row;
    gboolean reload;
    TIOError * io = NULL;
    THRegionLocation * return_value;
    THBaseServiceGetRegionLocationResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "row", &row,
                  "reload", &reload,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_REGION_LOCATION_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get_region_location (T_H_BASE_SERVICE_IF (self->handler),
                                                      &return_value,
                                                      table,
                                                      row,
                                                      reload,
                                                      &io,
                                                      error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_object_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "getRegionLocation",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getRegionLocation",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.getRegionLocation implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getRegionLocation",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (row != NULL)
      g_byte_array_unref (row);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_get_all_region_locations (THBaseServiceProcessor *self,
                                                             gint32 sequence_id,
                                                             ThriftProtocol *input_protocol,
                                                             ThriftProtocol *output_protocol,
                                                             GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceGetAllRegionLocationsArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_GET_ALL_REGION_LOCATIONS_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    TIOError * io = NULL;
    GPtrArray * return_value;
    THBaseServiceGetAllRegionLocationsResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_GET_ALL_REGION_LOCATIONS_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_get_all_region_locations (T_H_BASE_SERVICE_IF (self->handler),
                                                           &return_value,
                                                           table,
                                                           &io,
                                                           error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);
      if (return_value != NULL)
        g_ptr_array_unref (return_value);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "getAllRegionLocations",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getAllRegionLocations",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.getAllRegionLocations implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "getAllRegionLocations",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_process_check_and_mutate (THBaseServiceProcessor *self,
                                                     gint32 sequence_id,
                                                     ThriftProtocol *input_protocol,
                                                     ThriftProtocol *output_protocol,
                                                     GError **error)
{
  gboolean result = TRUE;
  ThriftTransport * transport;
  ThriftApplicationException *xception;
  THBaseServiceCheckAndMutateArgs * args =
    g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_MUTATE_ARGS, NULL);

  g_object_get (input_protocol, "transport", &transport, NULL);

  if ((thrift_struct_read (THRIFT_STRUCT (args), input_protocol, error) != -1) &&
      (thrift_protocol_read_message_end (input_protocol, error) != -1) &&
      (thrift_transport_read_end (transport, error) != FALSE))
  {
    GByteArray * table;
    GByteArray * row;
    GByteArray * family;
    GByteArray * qualifier;
    TCompareOp compareOp;
    GByteArray * value;
    TRowMutations * rowMutations;
    TIOError * io = NULL;
    gboolean return_value;
    THBaseServiceCheckAndMutateResult * result_struct;

    g_object_get (args,
                  "table", &table,
                  "row", &row,
                  "family", &family,
                  "qualifier", &qualifier,
                  "compareOp", &compareOp,
                  "value", &value,
                  "rowMutations", &rowMutations,
                  NULL);

    g_object_unref (transport);
    g_object_get (output_protocol, "transport", &transport, NULL);

    result_struct = g_object_new (TYPE_T_H_BASE_SERVICE_CHECK_AND_MUTATE_RESULT, NULL);
    g_object_get (result_struct, "success", &return_value, NULL);

    if (t_h_base_service_handler_check_and_mutate (T_H_BASE_SERVICE_IF (self->handler),
                                                   &return_value,
                                                   table,
                                                   row,
                                                   family,
                                                   qualifier,
                                                   compareOp,
                                                   value,
                                                   rowMutations,
                                                   &io,
                                                   error) == TRUE)
    {
      g_object_set (result_struct, "success", return_value, NULL);

      result =
        ((thrift_protocol_write_message_begin (output_protocol,
                                               "checkAndMutate",
                                               T_REPLY,
                                               sequence_id,
                                               error) != -1) &&
         (thrift_struct_write (THRIFT_STRUCT (result_struct),
                               output_protocol,
                               error) != -1));
    }
    else
    {
      if (io != NULL)
      {
        g_object_set (result_struct,
                      "io", io,
                      NULL);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndMutate",
                                                 T_REPLY,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (result_struct),
                                 output_protocol,
                                 error) != -1));
      }
      else
      {
        if (*error == NULL)
          g_warning ("THBaseService.checkAndMutate implementation returned FALSE "
                     "but did not set an error");

        xception =
          g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION,
                        "type",    *error != NULL ? (*error)->code :
                                   THRIFT_APPLICATION_EXCEPTION_ERROR_UNKNOWN,
                        "message", *error != NULL ? (*error)->message : NULL,
                        NULL);
        g_clear_error (error);

        result =
          ((thrift_protocol_write_message_begin (output_protocol,
                                                 "checkAndMutate",
                                                 T_EXCEPTION,
                                                 sequence_id,
                                                 error) != -1) &&
           (thrift_struct_write (THRIFT_STRUCT (xception),
                                 output_protocol,
                                 error) != -1));

        g_object_unref (xception);
      }
    }

    if (table != NULL)
      g_byte_array_unref (table);
    if (row != NULL)
      g_byte_array_unref (row);
    if (family != NULL)
      g_byte_array_unref (family);
    if (qualifier != NULL)
      g_byte_array_unref (qualifier);
    if (value != NULL)
      g_byte_array_unref (value);
    if (rowMutations != NULL)
      g_object_unref (rowMutations);
    g_object_unref (result_struct);

    if (result == TRUE)
      result =
        ((thrift_protocol_write_message_end (output_protocol, error) != -1) &&
         (thrift_transport_write_end (transport, error) != FALSE) &&
         (thrift_transport_flush (transport, error) != FALSE));
  }
  else
    result = FALSE;

  g_object_unref (transport);
  g_object_unref (args);

  return result;
}

static gboolean
t_h_base_service_processor_dispatch_call (ThriftDispatchProcessor *dispatch_processor,
                                          ThriftProtocol *input_protocol,
                                          ThriftProtocol *output_protocol,
                                          gchar *method_name,
                                          gint32 sequence_id,
                                          GError **error)
{
  t_h_base_service_processor_process_function_def *process_function_def;
  gboolean dispatch_result = FALSE;

  THBaseServiceProcessor *self = T_H_BASE_SERVICE_PROCESSOR (dispatch_processor);
  ThriftDispatchProcessorClass *parent_class =
    g_type_class_peek_parent (T_H_BASE_SERVICE_PROCESSOR_GET_CLASS (self));

  process_function_def = g_hash_table_lookup (self->process_map, method_name);
  if (process_function_def != NULL)
  {
    g_free (method_name);
    dispatch_result = (*process_function_def->function) (self,
                                                         sequence_id,
                                                         input_protocol,
                                                         output_protocol,
                                                         error);
  }
  else
  {
    dispatch_result = parent_class->dispatch_call (dispatch_processor,
                                                   input_protocol,
                                                   output_protocol,
                                                   method_name,
                                                   sequence_id,
                                                   error);
  }

  return dispatch_result;
}

static void
t_h_base_service_processor_set_property (GObject *object,
                                         guint property_id,
                                         const GValue *value,
                                         GParamSpec *pspec)
{
  THBaseServiceProcessor *self = T_H_BASE_SERVICE_PROCESSOR (object);

  switch (property_id)
  {
    case PROP_T_H_BASE_SERVICE_PROCESSOR_HANDLER:
      if (self->handler != NULL)
        g_object_unref (self->handler);
      self->handler = g_value_get_object (value);
      g_object_ref (self->handler);
      break;
    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (object, property_id, pspec);
      break;
  }
}

static void
t_h_base_service_processor_get_property (GObject *object,
                                         guint property_id,
                                         GValue *value,
                                         GParamSpec *pspec)
{
  THBaseServiceProcessor *self = T_H_BASE_SERVICE_PROCESSOR (object);

  switch (property_id)
  {
    case PROP_T_H_BASE_SERVICE_PROCESSOR_HANDLER:
      g_value_set_object (value, self->handler);
      break;
    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (object, property_id, pspec);
      break;
  }
}

static void
t_h_base_service_processor_dispose (GObject *gobject)
{
  THBaseServiceProcessor *self = T_H_BASE_SERVICE_PROCESSOR (gobject);

  if (self->handler != NULL)
  {
    g_object_unref (self->handler);
    self->handler = NULL;
  }

  G_OBJECT_CLASS (t_h_base_service_processor_parent_class)->dispose (gobject);
}

static void
t_h_base_service_processor_finalize (GObject *gobject)
{
  THBaseServiceProcessor *self = T_H_BASE_SERVICE_PROCESSOR (gobject);

  thrift_safe_hash_table_destroy (self->process_map);

  G_OBJECT_CLASS (t_h_base_service_processor_parent_class)->finalize (gobject);
}

static void
t_h_base_service_processor_init (THBaseServiceProcessor *self)
{
  guint index;

  self->handler = NULL;
  self->process_map = g_hash_table_new (g_str_hash, g_str_equal);

  for (index = 0; index < 20; index += 1)
    g_hash_table_insert (self->process_map,
                         t_h_base_service_processor_process_function_defs[index].name,
                         &t_h_base_service_processor_process_function_defs[index]);
}

static void
t_h_base_service_processor_class_init (THBaseServiceProcessorClass *cls)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (cls);
  ThriftDispatchProcessorClass *dispatch_processor_class =
    THRIFT_DISPATCH_PROCESSOR_CLASS (cls);
  GParamSpec *param_spec;

  gobject_class->dispose = t_h_base_service_processor_dispose;
  gobject_class->finalize = t_h_base_service_processor_finalize;
  gobject_class->set_property = t_h_base_service_processor_set_property;
  gobject_class->get_property = t_h_base_service_processor_get_property;

  dispatch_processor_class->dispatch_call = t_h_base_service_processor_dispatch_call;
  cls->dispatch_call = t_h_base_service_processor_dispatch_call;

  param_spec = g_param_spec_object ("handler",
                                    "Service handler implementation",
                                    "The service handler implementation "
                                    "to which method calls are dispatched.",
                                    TYPE_T_H_BASE_SERVICE_HANDLER,
                                    G_PARAM_READWRITE);
  g_object_class_install_property (gobject_class,
                                   PROP_T_H_BASE_SERVICE_PROCESSOR_HANDLER,
                                   param_spec);
}
